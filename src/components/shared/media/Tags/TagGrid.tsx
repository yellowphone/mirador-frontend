import React, { FC } from 'react';
import { TagGridDataProps } from './Tag.types';
import { Tag } from './Tag';

export const TagGrid: FC<TagGridDataProps> = ({ tags }) => {

    // using a list of tags, render many one and/or more tags

    return (
        <>
        </>
    )
}